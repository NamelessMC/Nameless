FROM phpdoc/phpdoc as doc_builder

COPY . /source

RUN mkdir /target && \
    phpdoc -d /source -t /target -i /vendor

FROM nginxinc/nginx-unprivileged:stable-alpine

COPY --from=doc_builder /target /usr/share/nginx/html
